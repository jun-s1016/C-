#include <iostream>
#include "../Program/Zergling.h"
#include "../Program/Hydralisk.h"
#include "../Program/Lurker.h"
#include "../Program/Beacon.h"

using namespace std;

int main()
{
#pragma region 업 캐스팅

	/*Beacon beacon;

	Zerg* zergling = new Zergling();
	Zerg* hydralisk = new Hydralisk();
	Zerg* lurker = new Lurker();

	zergling->SetHP(10);
	hydralisk->SetHP(15);
	lurker->SetHP(20);

	cout << "Zergling 의 현재 체력 : " << zergling->GetHP() << endl;
	cout << "hydralisk 의 현재 체력 : " << hydralisk->GetHP() << endl;
	cout << "lurker 의 현재 체력 : " << lurker->GetHP() << endl;

	beacon.Trigger(zergling);
	beacon.Trigger(hydralisk);
	beacon.Trigger(lurker);

	cout << "Zergling 의 현재 체력 : " << zergling->GetHP() << endl;
	cout << "hydralisk 의 현재 체력 : " << hydralisk->GetHP() << endl;
	cout << "lurker 의 현재 체력 : " << lurker->GetHP() << endl;*/

#pragma endregion

	return 0;
}